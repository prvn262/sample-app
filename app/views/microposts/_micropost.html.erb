<hr>
<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 20), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content">
    <%= micropost.content %>
    <% if micropost.image.attached? %>
      <%= image_tag micropost.display_image, class: "micropost-image" %>
    <% end %>
  </span>

  <span class="timestamp">Posted <%= time_ago_in_words(micropost.created_at) %> ago. </span>
  <% if current_user.admin? || current_user  == micropost.user %>
    <%= link_to "delete", micropost, data: { turbo_method: :delete, turbo_confirm: "You Sure?" }, class: "btn btn-sm btn-danger" %>
  <% end %>
</li>
<hr>




<%# time_ago_in_words helper method comes from ActionView::Helpers::DateHelper %>
<%# if current_user.admin? && current_user != user  %>

<%# current_user  == micropost.user %>
