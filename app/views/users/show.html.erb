<%= provide(:title, @user.name) %>
<%= session[:user_id] %>

<div class="row">
		<section class="user_info" style="margin: 50px;">
	  	<h1>
				<%= gravatar_for @user %> 
				<%= @user.name %>
      </h1> 
  <aside class="col-md-4">
  	</section>
  </aside> 
</div>
